#include<stdio.h>
#include<conio.h>
void quick(int A[10]; int low; int high);
partition(int A[10], low, high)
void main()
{
int low,high,mid,n,A[10];
printf("enter the size");
scanf("%d",&n);
printf("enter the elements");
for(i=0;i<n;i++)
	printf("%d",&A[i]);
getch();
}
void quick(int A[10]; int low; int high);
{
if(low<high)
{
mid=partition(int A[10], int low; int high)
quick(a, low, high);
quick(a, low, mid-1);
quick(a, mid+1, high);
}
}
void partition(int A[],int low, int high)
{

}
